{
  "name": "zk-ballot",
  "version": "1.0.0",
  "description": "Decentralized anonymous voting system with zero-knowledge proofs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:testnet": "hardhat run scripts/deploy.js --network sepolia",
    "clean": "hardhat clean"
  },
  "keywords": ["voting", "DAO", "governance", "zero-knowledge", "privacy", "blockchain"],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "keccak256": "^1.0.6",
    "merkletreejs": "^0.3.11"
  }
}
